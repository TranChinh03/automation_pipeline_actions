# .github/workflows/auto-promote.yaml
name: 🚀 Auto Promote (Dynamic Environment Mapping & Auto-Merge)

# Trigger only when a Pull Request is successfully merged (closed event)
on:
  pull_request:
    types: [closed]
    branches:
      - dev
      - sit
      - uat
      - staging
      - production
      - main
      - release/**

# Required permissions for the GitHub Actions bot
permissions:
  contents: write    
  pull-requests: write 

jobs:
  promote:
    # Only run if the PR was merged (not just closed)
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest

    steps:
      - name: ⬇️ Checkout Repository (Full History)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🛠️ Install jq & GitHub CLI
        run: |
          sudo apt-get update
          sudo apt-get install -y jq gh

      # ----------------------------------------------------------------------
      # 1. Load Environment Configuration (Logic Names)
      # ----------------------------------------------------------------------
      - name: ⚙️ Load Environment Configuration
        id: load-envs
        run: |
          echo "🔍 Loading environment list from .github/environments.json"
          if [ ! -f ".github/environments.json" ]; then
            echo "❌ File .github/environments.json does not exist! Please create it."
            exit 1
          fi
          
          # Extract all logic names into a space-separated string
          environments=$(jq -r '.environments[].name' .github/environments.json | tr '\n' ' ')
          
          if [ -z "$environments" ]; then
            echo "❌ File .github/environments.json trống hoặc không chứa environments hợp lệ!"
            exit 1
          fi
          
          echo "Read environments (logic names): $environments"
          echo "environments=$environments" >> $GITHUB_OUTPUT

          # Save the full JSON config to a file for later lookup (required for mapping)
          jq -c . .github/environments.json > environments.json
          echo "config_file_path=environments.json" >> $GITHUB_OUTPUT

      # ----------------------------------------------------------------------
      # 2. Extract selected LOGIC NAMES from the PR Body
      # ----------------------------------------------------------------------
      - name: 📝 Extract Target Environments from PR body
        id: extract
        env:
          PR_BODY: ${{ github.event.pull_request.body }}
          ENVIRONMENTS: ${{ steps.load-envs.outputs.environments }} 
        shell: bash
        run: |
          set -e
          body="$PR_BODY"
          
          CLEAN_ENVIRONMENTS=$(echo "$ENVIRONMENTS" | tr '\n' ' ' | tr -s ' ')
          IFS=' ' read -r -a env_array <<< "$CLEAN_ENVIRONMENTS"
          selected_logic_names=()

          for env in "${env_array[@]}"; do
            echo "🔸 Checking for logic name: [$env]"
            # Secure Regex pattern: searches for a line containing "- [x] logic_name"
            if echo "$body" | grep -iq "^\s*-\s*\\[x\\]\s*${env}"; then
              selected_logic_names+=("$env")
              echo "   ➡️ Found!"
            fi
          done

          if [ ${#selected_logic_names[@]} -eq 0 ]; then
            echo "⚠️ No target environments selected."
            echo "selected_names=" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          echo "✅ Selected logic names: ${selected_logic_names[*]}"
          echo "selected_names=${selected_logic_names[*]}" >> "$GITHUB_OUTPUT"

      - name: 🛑 Stop if no target branches
        if: steps.extract.outputs.selected_names == ''
        run: echo "⏹️ No target environments selected. Skipping."

      # ----------------------------------------------------------------------
      # 3. Configure Git and Get Commit SHA
      # ----------------------------------------------------------------------
      - name: 🤖 Configure Git
        if: steps.extract.outputs.selected_names != ''
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: 🆔 Get Merge Commit SHA
        id: commit
        run: |
          sha=$(git rev-parse HEAD)
          echo "💡 Merge commit SHA: $sha"
          echo "sha=$sha" >> $GITHUB_OUTPUT

      # ----------------------------------------------------------------------
      # 4. Promote Code (Mapping Logic Name to Physical Branch) & Auto-Merge
      # ----------------------------------------------------------------------
      - name: ➡️ Promote to Selected Environments
        if: steps.extract.outputs.selected_names != '' 
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          sha=${{ steps.commit.outputs.sha }}
          base_branch=${{ github.base_ref }}
          LOGIC_NAMES=${{ steps.extract.outputs.selected_names }}
          
          # Load the full configuration JSON
          CONFIG=$(cat environments.json)
          
          IFS=' ' read -r -a names_to_promote <<< "$LOGIC_NAMES"
          
          for logic_name in "${names_to_promote[@]}"; do
            
            # Use jq to lookup the PHYSICAL branch name from the LOGIC name
            target_branch=$(echo "$CONFIG" | jq -r --arg name "$logic_name" '.environments[] | select(.name == $name) | .branch')
            
            if [ -z "$target_branch" ]; then
                echo "❌ Error: Could not find physical branch for environment '$logic_name'."
                continue
            fi
            
            echo "🔹 Promoting '$logic_name' (Physical branch: $target_branch)..."
            
            # Define branch and title using the LOGIC NAME
            promotion_branch="promote-$base_branch-to-$logic_name" 
            PR_TITLE="Promote $base_branch → $logic_name"

            # 1. Fetch and Checkout the PHYSICAL target branch
            git fetch origin "${target_branch}" 2>/dev/null || echo "⚠️ Warning: Could not fetch ${target_branch}."
            git checkout -b "${promotion_branch}" origin/"${target_branch}" || git checkout -b "${promotion_branch}"

            # 2. CHERRY-PICK (Uses the -m 1 flag to handle merge commits)
            if git cherry-pick -m 1 "$sha"; then
                echo "✅ Cherry-pick succeeded."
            else
                echo "❌ Cherry-pick failed. Attempting cleanup..."
                git cherry-pick --abort 2>/dev/null || true 
                echo "⚠️ Automatic promotion skipped due to conflict on ${target_branch}."
                continue
            fi

            # 3. Push promotion branch
            git push origin "${promotion_branch}" -f

            # 4. Create PR and get the PR URL/Number
            echo "📦 Creating promotion PR..."
            PR_URL=$(gh pr create \
              --base "${target_branch}" \
              --head "${promotion_branch}" \
              --title "$PR_TITLE" \
              --body "### 🚀 Automatic Promotion. The merge commit \`$sha\` from branch \`$base_branch\` has been applied to branch \`$target_branch\` (Logic Name: $logic_name). Please Review and Merge. --- *Automatically generated by GitHub Actions.*")

            echo "✅ PR created: $PR_URL"
            
            # 5. ATTEMPT AUTO-MERGE (Queues for merge once checks pass)
            echo "Attempting to auto-merge PR..."
            gh pr merge "$PR_TITLE" \
              --repo "${{ github.repository }}" \
              --squash \
              --auto 
            
            echo "🎉 PR set for auto-merge once checks pass."

          done